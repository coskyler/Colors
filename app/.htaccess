RewriteEngine On
RewriteBase /

# If the request matches an existing file or folder, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, send everything to index.php
RewriteRule ^ index.php [QSA,L]